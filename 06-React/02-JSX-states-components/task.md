Begginer – Create a div, on hover, the div changes background color

Medium - Create a red small div that follow the cruser x and y positions

Advanced – create a Counter component. The counter allows to add 2, remove 2.In the same components, count how many times the buttons were clicked.
