// תרגיל 1
for (let i= 99; i > 0 ; i--) {
    const x = i -1;
      document.write(`${i} Bottles of Beer on the wall. Take one down pass it around, and ${x} bottles of beer on the wall.` + "<br>");
  };

// תרגיל 2
