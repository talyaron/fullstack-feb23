// // create a function, which takes 2 numbers (a, b),
// // and return the multiplication of these 2 numbers.
// // run the function to test it. as a prompt
function multiply(a, b) {
    var sum = (a * b);
    return sum;
}
console.log(multiply(4, 5));
