"use strict";

var userNumber = Number(prompt("please write some numbers") || 0);
debugger;
var r = userNumber;
var o = 0;

for (var x = 1; r >= 1; x++) {
  if (r == userNumber) {
    o = r / 10;
  } else {
    o = o / 10;
  }
}

for (var z = 0; z <= x; z++) {
  var arr = [x];
  console.log(arr);
} // function sortingNumbers() {
//   // 1324
// }