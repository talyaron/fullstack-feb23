// Exercise 1: Create an array called "colors" with three different color names. Print the second element of the array.
const color = [`black`, `white`, `red`];
console.log(color[1]);
// Create an array called "numbers" with five random numbers. Add two more numbers to the end of the array.Print the updated array.
const numbers = [1, 8, 10, 19, 22];
numbers[5] = 100;
numbers[6] = 122;
console.log(numbers);

for (let i = 0; i < numbers.length; i++) {
  console.log(numbers[i]);
}
// example
