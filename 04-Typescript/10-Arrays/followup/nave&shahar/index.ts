const ages = [18,25,22,25,100]
const index = ages.every((age) => age > 17);

console.log(index);
 const fruits = [  `banana`, `apple`, `mango`,`melon`, `watermelon` ]
 const sort = fruits.slice(1,4);
 
 console.log(sort);
 