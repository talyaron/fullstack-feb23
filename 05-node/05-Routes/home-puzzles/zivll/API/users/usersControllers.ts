import { User, users } from "./usersModel";

export function addUser(user: User) {
  try {
    console.log(user);
  } catch (error) {
    console.error(error);
  }
}
export function signIn() {
  try {
  } catch (error) {
    console.error(error);
  }
}
