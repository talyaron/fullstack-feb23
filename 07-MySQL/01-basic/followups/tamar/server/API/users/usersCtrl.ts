import express from 'express'

export const gerAllUsers = async (req: express.Request, res: express.Response) => {
    try {
        const query = 
    } catch (error) {
        console.error(error)
    }
}